{
  "name": "@ai2web/connector",
  "version": "0.1.0",
  "description": "Agent-side AI2Web connector - an MCP server that fronts the Discovery Network so Claude/ChatGPT can find AI-ready sites and act on them.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ai2web-connector": "./dist/server.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "publishConfig": { "access": "public" },
  "dependencies": {
    "@ai2web/core": "0.1.0",
    "@ai2web/mcp-bridge": "0.1.0"
  },
  "peerDependencies": {
    "@modelcontextprotocol/sdk": ">=1.0.0"
  },
  "scripts": {
    "build": "tsc --build"
  }
}
